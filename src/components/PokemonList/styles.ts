import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  centerContent: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
  },
  listStyle: {
    flex: 1,
    paddingBottom: 20,
  },
  footerPadding: {
    paddingVertical: 20,
  },
});
